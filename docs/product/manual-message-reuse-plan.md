# 직접 입력 문구 보관함 — 기획/계획서 (초안, 2026-09-06)

> **한 줄**: 한 번 만든 직접 입력 문구는 다시 만들지 않는다. 기록은 남기고, 오디오는
> **알람이 아니라 목소리**에 매달아 둔다.
>
> 상태: **미구현 · 결정 대기.** 아래 「지금 어떻게 도는가」는 코드에서 확인한 사실이고,
> 「바꾸려는 것」부터가 제안이다.

## 0. 지금 어떻게 도는가 (2026-09-06 코드 확인)

확인한 파일을 함께 적는다 — 이 문서가 낡았는지 판단할 근거다.

### 0-1. 월 한도는 **생성할 때만** 깎인다 — 같은 문구는 공짜다

- 한도: 개인 30 / 커플 50 / 가족 100, **KST 월 단위**, 커플·가족은 그룹이 한 풀을
  나눠 쓴다 (`packages/backend/src/lib/manual-tts-quota.ts`).
- `POST /tts/generate` 는 **캐시를 먼저 찾고**, 못 찾았을 때만 `reserveManualTtsQuota`
  로 카운터를 올린다 (`packages/backend/src/routes/tts.ts`). 생성이 실패하면 환불한다.
- 즉 **같은 문구 = 차감 없음**이 이미 규칙이다. 단 '같다' 의 기준은 캐시 키다:
  제공자 · 제공자 보이스 id · 목소리 프로필 id · 모델 · 언어 · **정규화한 문구** · 출력
  포맷 (`packages/backend/src/lib/audio-cache.ts` 의 `computeTtsCacheKey`).
  → 같은 문구라도 **목소리를 바꾸면 새 생성**이다(그게 맞다 — 다른 소리를 만드는 것이니).
- 직접 입력은 **사용자끼리 캐시를 공유하지 않는다**(`anyUser: isSystemVoice &&
  !isManualGeneration`). 공유하면 남의 `messages` 행을 물어 저장이 404 로 막히고,
  한도도 안 깎이는 구멍이 된다.
- 앱은 대개 서버까지 가지도 않는다. 같은 기기에서 같은 문구를 다시 치면 로컬 입력 캐시
  (`AlarmAudioStore.ttsInputKey` + `linkTtsInput`)가 파일을 그대로 돌려준다.

### 0-2. 보관함 테이블은 **이미 있다**

- `message_library(id, user_id, message_id, is_favorite, received_at)` — 초안 미리듣기가
  아닌 모든 생성이 여기에 한 줄을 남긴다(`routes/tts.ts`).
- `GET /tts/messages` 가 그 목록을 준다(목소리·카테고리 필터, 페이지네이션, 프리셋·삭제된
  목소리 제외).
- ⚠ **두 앱 어디에서도 이 목록을 쓰지 않는다.** 앱이 부르는 건 개별 오디오
  (`GET /tts/messages/:id/audio`) 뿐이다. 즉 **데이터는 쌓이는데 보여 주는 화면이 없다.**

### 0-3. 오디오는 30일이면 사라진다

`packages/backend/src/lib/audio-retention.ts` (`cleanupExpiredAudio`):

- `generated_audio_assets` 는 **생성 30일** 뒤 정리 대상이다. 지울 때 R2 오브젝트를
  삭제 큐에 넣고 `messages.audio_url` 을 비운다.
- 예외 둘: **알람이 그 메시지를 참조 중**이거나, **프리셋(`is_preset=1`)** 이면 건너뛴다.
- 기준이 `created_at` 이라, 알람에서 떼어 낸 문구는 **아무리 자주 써도** 만든 지 30일이면
  사라진다.

### 0-4. **지우는 문은 이미 목소리 축이다** — 동의 철회·해지도 그 길로 간다

- 동의 철회(`user.ts` → `deleteSensitiveVoiceDataForUser`)는 **내가 등록한 클론 프로필**을
  축으로 지운다: 그 목소리의 `generated_audio_assets`·`message_library`·`messages`·
  `voice_uploads`·`voice_profiles` + R2·ElevenLabs 삭제 큐.
  ⚠ 예전에는 `user_id` 축도 함께 걸려 있어 **시스템 목소리로 만든 문구까지** 지웠고, 그건
  고쳐졌다(같은 파일 주석).
- 요금제 해지는 **즉시 삭제가 아니다.** 무료로 내려갈 때는 제공자 클론만 반납하고
  (`evicted_*` 표식) 데이터는 유예 동안 남는다 — 그 사이 재구독하면 재클론으로 돌아온다.
  유예가 지나면 `sweepPaidVoiceRetention` 이 같은 `deleteSensitiveVoiceDataForUser` 를 부른다.
- 탈퇴는 `purgeUserAccount` 가 계정 전체를 지운다.

→ 즉 **"목소리 삭제 → 오디오 삭제" 는 이미 서버의 구조**다. 새로 만들 것이 아니라
**문서로 고정하면 된다**(아래 0-5 도 마찬가지다).

### 0-5. 로컬 오디오는 **참조 카운트**로 지운다 — 양쪽 앱 모두

- 안드로이드: `AlarmRepository.deleteAlarm` → `deleteCachedAudioIfUnreferenced` —
  `alarmDao.countByAudioCacheKey(cacheKey) > 0` 이면 **안 지운다.**
- iOS: `LocalAlarmStore.delete` 가 `countByAudioCacheKey(...) == 0` 일 때만 캐시 키를
  돌려주고, 그때만 `AudioCacheStore.deleteCachedAudio` 가 돈다.
- 즉 **"다른 알람이 아직 쓰면 남긴다"** 는 이미 양쪽에 있다. 새로 만들 규칙이 아니다.
- 로컬 재사용 판정도 이 파일에 달려 있다: 입력 별칭(`resolveTtsInput`)이 있어도
  **파일이 없으면**(`getCachedAudio` → null) 서버로 간다(`AlarmEditorScreen` 저장 경로).
  별칭 파일은 오디오와 이름이 달라 함께 지워지지 않지만, 이 이중 검사 덕에 문제가 되지 않는다.
- 목소리를 지우거나 유료가 끊기면 서버도 그 목소리의 `messages`·`message_library`·
  `generated_audio_assets` 를 지우고 R2 삭제를 큐에 넣는다
  (`packages/backend/src/lib/paid-voice-cleanup.ts`).

**정리**: 재사용은 **이미 되고 있다.** 빠진 것은 셋이다 — ① 그걸 보여 주는 화면,
② 서버 오디오가 30일이면 사라진다는 점, ③ 한도의 뜻(§3-1)이 코드와 문서에 정해져 있지 않다.
로컬 참조 카운트 삭제와 목소리 축 삭제는 **이미 원하는 모양**이라 손대지 않는다.

## 1. 바꾸려는 것 (2026-09-06 결정)

1. **보관함을 화면으로 꺼낸다.** 직접 입력 화면에서 이전에 쓴 문구를 고를 수 있게 한다.
2. **서버 오디오는 목소리에 매단다.** **목소리가 살아 있는 동안 계속 보관한다 —
   기간·개수 상한을 두지 않는다.** (로컬은 다르다 — 4번.)
   - 이유는 비용 절감이 아니라 **경험의 동일성**이다. 재합성은 같은 문구라도 소리가 미묘하게
     달라진다. 오래 쓴 알람이 어느 날 다른 억양으로 울리지 않게 하려면 그때 만든 바이트를
     그대로 들고 있어야 한다.
3. **DB 기록은 스토리지보다 오래 산다.** 어떤 이유로 오디오 바이트가 사라져도
   `messages`·`message_library` 행은 남는다(그 문구를 다시 만들 때 근거가 된다).
   행까지 사라지는 경우는 **목소리가 사라질 때** 하나뿐이다(아래 4).
4. **지우는 문은 하나다 — 목소리 삭제.** 동의 철회·요금제 해지·탈퇴는 **목소리를 지우고**,
   목소리 삭제가 그 목소리의 오디오·문구·보관함 행을 지운다.
5. **한도는 '새로 만드는 횟수'다.** 오디오가 남아 있어 재합성을 안 하더라도, **새 알람에
   문구를 새로 붙이면 1회 차감**한다(아래 §3-1).
6. **폰 저장소는 참조 카운트 그대로 둔다**(2026-09-06 정정 — 초안에서는 "알람을 지워도
   로컬 파일을 남긴다" 고 썼는데 되돌린다). 그 알람을 지웠고 다른 알람도 안 쓰면 파일은
   지운다. 폰은 **지금 쓰는 것만** 들고, 오래 보관하는 쪽은 서버다.
   → 그래서 **"글자가 같으면 서버를 안 불러도 된다" 는 그 오디오를 쓰는 알람이 하나라도
   남아 있을 때의 이야기**가 맞다. 그게 없어지면 파일도 없어지고, 다시 쓰려면 서버에서
   받아온다(온라인일 때).
7. **오프라인에서는 없는 문구를 못 만든다.** 저장을 누른 순간 **요청을 보내기 전에**
   로컬에서 판정해 모달로 막는다(§3-4).

## 2. 데이터 모델 (append-only)

`message_library` 에 **컬럼 하나**만 더한다. 테이블을 새로 만들지 않는다 — 이미 모든
생성이 여기 한 줄을 남기고 있어 이력이 이어진다.

| 컬럼 | 뜻 |
| --- | --- |
| `last_used_at` | 알람에 붙은 마지막 시각. 목록 정렬과 '언제 썼는지' 표시 |
| `alarm_count` | 이 문구로 만든 알람 수(선택, 목록 맥락용) |

**활성/비활성 상태 컬럼은 두지 않는다.** 보관 상한을 없앴으므로 상태가 갈릴 일이 없다 —
목소리가 있으면 문구도 오디오도 있고, 목소리가 없으면 셋 다 없다. (앞선 초안에서 제안한
'오디오 유무로 유도' 도 이제 불필요하다. 그 판정은 **차감**과 무관하고 — 차감은 §3-1 대로
'새로 만들었는가' 로 정한다 — 우리 쪽 재합성 여부를 가르는 내부 사정일 뿐이다.)

- 마이그레이션은 **제자리 추가**뿐이다(CLAUDE.md 「prod DB 전체 초기화는 하지 않는다」).
  `last_used_at` 은 `received_at` 으로 백필한다.
- 새 컬럼을 읽는 경로는 **fail-closed** 로 둔다(배포가 마이그레이션보다 먼저 도는 창).

## 3. 규칙

### 3-1. 차감 — **폰에 없어서 서버에 만들어 달라고 할 때만**

> **상태: 구현됨**(2026-09-07). 동작 규칙의 단일 출처는
> [`docs/spec/voice-and-message.md`](../spec/voice-and-message.md) §8 이다.

기준은 하나다: **저장하려는 순간 그 문구의 오디오가 이 폰에 있는가.**

| 상황 | 차감 |
| --- | --- |
| 폰에 있다(그 오디오를 쓰는 알람이 하나라도 남아 있다) — 몇 번을 쓰든 | **없음** |
| 폰에 없다 → 서버에 요청한다(서버 캐시 히트든 새 합성이든) | **1회** |
| 오프라인인데 폰에 없다 | 저장 자체를 막는다(§3-4) → 차감할 일이 없다 |

- 지금과 다른 점은 **캐시 히트도 센다**는 것 하나다. 지금은 서버 캐시가 맞으면 카운터를
  건너뛰는데(§0-1), 사용자에게 "우리 서버에 남아 있었는지" 는 보이지 않는 사정이다.
  **폰에 없으면 새로 만드는 것**이라는 감각이 훨씬 단순하다.
- 그래서 **오프라인 차감 문제가 사라진다.** 오프라인에서 저장되는 것은 폰에 이미 있는
  문구뿐이고, 그건 무료다. 차감이 필요한 경우는 반드시 온라인이라 그 자리에서 처리된다.

⚠ **기기를 바꾸면 빈 상태로 시작한다**(2026-09-06 코드 확인 — 앞선 초안의 '복구' 가정은
틀렸다). pull 은 **받은 알람만** 가져온다(안드로이드 `RemoteAlarmPullSyncService` 의
`filter { it.isReceived }`, iOS `isReceivedRemoteCandidate`). 내 알람은 기기 로컬이 원본이라
새 폰에 따라오지 않고, **기기마다 다른 알람을 둘 수 있다.**

그래서 '복구는 무료' 라는 갈래는 **존재하지 않는다.** 새 기기에서는 알람을 새로 만들게
되고, 그때 예전 문구를 다시 치면 위 표대로 **차감된다.**
 - 우리 비용은 그대로 0 이다 — 서버 보관함에 그 오디오가 있으면 재합성 없이 그대로 준다.
   사용자도 **같은 소리**를 돌려받는다(§1-2 의 이유).
 - 대신 보관함 목록(§3-3)이 새 기기에서 특히 쓸모 있다 — 예전 문구를 다시 타이핑하지 않고
   고르기만 하면 된다.

### 3-2. 수명

| 대상 | 지금 | 바꾼 뒤 |
| --- | --- | --- |
| 서버 오디오(R2) | 생성 30일 (알람 참조·프리셋 제외) | **그 목소리가 살아 있는 동안 계속 보관.** 기간·개수 상한 없음 |
| 서버 DB 행(`messages`·`message_library`) | 오디오와 함께 정리되기도 한다 | **오디오가 사라져도 남는다.** 목소리가 사라질 때만 지운다 |
| 로컬 오디오 | 그 오디오를 쓰는 알람이 하나도 없으면 삭제(참조 카운트) | **그대로 둔다.** 폰은 지금 쓰는 것만 들고, 오래 보관은 서버가 한다 |
| 지우는 문 | 여러 갈래(알람 삭제·TTL·목소리 삭제·철회·해지) | **목소리 삭제 하나.** 동의 철회·요금제 해지·탈퇴는 목소리를 지우고, 그 뒤는 같은 경로 |

⚠ **상한을 없앤 대가는 저장 비용이다.** 유료 사용자 1인이 한 달 한도를 다 쓰면 문구
30~100개, 문장 하나가 수십 KB 수준이라 개인 단위로는 작다. 다만 **삭제되지 않는 한 계속
쌓이므로**, 총량 모니터링(사용자당 오브젝트 수·바이트)은 붙여 두는 것이 좋다. 상한을 다시
두게 되면 그때는 이 표부터 고친다.

### 3-3. 화면

- 직접 입력 다이얼로그 아래 **'이전에 쓴 문구'** 목록(최근순, 그 목소리 것만).
  - 고르면 즉시 적용된다(기다림 없음 — 오디오가 이미 있다).
  - 다만 **새 알람에 붙이는 것이면 1회 차감**이므로, 목록 위에 남은 횟수를 함께 보여
    준다("이번 달 27/30 남음"). 고르는 순간이 아니라 **저장할 때** 깎인다.
- 목록에서 **지우기**를 준다. 사용자가 지운 것은 기록까지 없앤다 — 「기록은 남긴다」는
  **시스템이 정리할 때**의 규칙이지, 사용자의 삭제까지 막는 규칙이 아니다.
- 목소리를 지우면 그 목소리의 목록도 함께 사라진다(§3-2 의 '지우는 문' 하나).

### 3-4. 오프라인

판정은 **로컬에서** 한다 — 요청을 보내 볼 필요가 없다.

| 상황 | 동작 |
| --- | --- |
| 그 문구의 오디오가 **폰에 있다**(별칭 + 파일 둘 다) | 그대로 저장(지금과 같다) |
| 폰에 없고 **온라인** | 서버에서 받아오거나 새로 만든다 |
| 폰에 없고 **오프라인** | **저장을 누르는 순간 모달로 막는다** — 요청은 보내지 않는다 |

- 판정식은 저장 경로가 이미 쓰는 것과 같다: `resolveTtsInput(inputKey)` 로 별칭을 찾고
  `getCachedAudio(cacheKey)` 가 **파일까지** 확인될 때만 '있다'. 별칭만 있고 파일이 없으면
  없는 것이다.
- 막는 방식은 **기존 규약 그대로** — 저장 버튼을 죽이지 않고, 누르면 이유를 알럿으로
  말한다(`SaveBlockReason` 에 갈래 하나 추가). CLAUDE.md 「저장 뒤 검은 화면」 절의
  "죽은 버튼은 이유를 알려 주지 않아 고장으로 읽힌다" 와 같은 규칙이다.
- 문구: "지금은 연결이 없어 새 문구를 만들 수 없어요. 연결되면 다시 저장해 주세요."

⚠ **이 규칙이 §5 의 '오프라인 차감' 문제를 반쯤 없앤다.** 오프라인에서 새로 만들 수 없으니
차감할 새 합성도 없다. 남는 것은 **이미 폰에 있는 문구를 오프라인에서 새 알람에 붙이는**
경우 하나뿐이고, 그건 §4 의 이벤트 큐로 온라인 때 보내면 된다.

## 4. 사용 기록(이벤트 로그) — 직접 입력과 무관하게

> **상태: 구현됨**(2026-09-07). 서버 `POST /api/events` + `usage_events`(마이그레이션 #112),
> 안드로이드 Room 큐 + `UsageEventUploadWorker`, iOS 파일 큐 + `UsageEventUploader`.
> 동작 규칙은 [`docs/spec/usage-events.md`](../spec/usage-events.md) 가 단일 출처다.
> **보관 1년**으로 정했고 처리방침 3장·cron 정리까지 반영했다.

> 지시(2026-09-06): "생성 시 DB 에 기록이 쌓이게 하고 싶다. 직접 입력과 무관하게 모든 것
> (알람 울리는 것도). 오프라인이면 기록해 뒀다가 온라인 때 보내도록."

### 4-1. 무엇을 남기나

`usage_events` 한 테이블에 append-only 로 쌓는다.

| 컬럼 | 뜻 |
| --- | --- |
| `id` | 클라가 만드는 UUID. **재전송해도 중복되지 않는 근거**(UNIQUE) |
| `user_id` | 소유자 |
| `type` | `alarm_created` / `alarm_updated` / `alarm_deleted` / `alarm_rang` / `alarm_dismissed` / `alarm_snoozed` / `manual_message_attached` / `voice_created` … |
| `occurred_at` | **기기에서 일어난 시각**(오프라인 지연과 무관하게 이게 진실이다) |
| `received_at` | 서버가 받은 시각 |
| `payload_json` | 알람 id·목소리 id·문구 id 등 **식별자만** |

⚠ **문구 원문을 이벤트에 넣지 않는다.** 문구는 이미 `messages` 에 있고, 로그에 사본을
만들면 삭제(목소리 삭제·철회) 때 지워야 할 곳이 하나 더 늘어난다. 식별자만 넣는다.

### 4-2. 오프라인

- 앱이 로컬 큐에 적고(안드로이드 Room 테이블 / iOS 파일), 온라인이 되면 **배치로** 보낸다.
  알람 동기화가 이미 쓰는 길(WorkManager·`RemoteAlarmPullSync`)에 얹는다.
- `id` 가 UNIQUE 라 재전송이 안전하다(멱등). 서버는 이미 받은 id 를 조용히 무시한다.
- ⚠ **울릴 때 네트워크를 부르지 않는다.** 알람 경로는 로컬·오프라인이 원칙이다
  (CLAUDE.md 「Core Values — Real alarm」). 울림은 **로컬에만 적고**, 전송은 그 뒤에 한다.

### 4-3. 정해야 할 것

- **개인정보 처리방침에 수집 항목·목적·보관 기간을 넣어야 한다.** 울림·해제 기록은
  행동 데이터다. 지금 방침에 없는 항목이라 문서 개정이 선행이다(그리고 문서 버전은
  스토어 릴리스와 짝을 맞춘다 — CLAUDE.md 배포 절).
- **보관 기간.** append-only 라 무한히 는다. 1년 뒤 원본 삭제 같은 기준이 필요하다.
- **용도.** 통계용인지, 화면에 보여 줄 '기록' 인지에 따라 필요한 필드가 달라진다.
  화면에 보여 줄 것이면 `alarm_rang` 에 알람 라벨 스냅샷이 필요할 수 있다(그러면 위
  '식별자만' 규칙과 부딪힌다 — 그때 다시 정한다).

## 5. 구현 순서

1. **서버 — 보관**: `cleanupExpiredAudio` 의 TTS 정리에서 **직접 입력 메시지를 제외**한다
   (목소리가 살아 있으면 보관). 프리셋 예외는 그대로. 지금 로직이 `messages.audio_url` 을
   비우고 자산 행을 지우므로, 여기만 고치면 §3-2 의 보관 규칙이 선다.
2. **서버 — 기록**: `message_library.last_used_at` 추가 + 백필, `GET /tts/messages` 응답에
   싣는다. 목소리 삭제 경로(`deleteSensitiveVoiceDataForUser`·`deletePaidVoiceDataForUser`)는
   **이미 목소리 축으로 지운다** — 손댈 곳이 없다.
3. **서버 — 차감**: '문구를 알람에 새로 붙였다' 를 세는 자리를 만든다(알람 생성·수정
   경로에서 직접 입력 문구가 새로 붙었을 때). 합성 여부와 분리하고, 캐시 히트에도 센다.
   기존 `reserveManualTtsQuota` 는 그대로 두되 **호출 지점을 옮기는 셈**이다.
4. **앱(양쪽) — 삭제**: 알람 삭제 시 오디오를 지우지 않게 한다
   (`deleteCachedAudioIfUnreferenced` 호출 제거). 목소리 삭제 경로에서만 지운다.
5. **앱(양쪽) — 화면**: 직접 입력 화면에 보관함 목록(`GET /tts/messages`).
6. **앱(양쪽) — 오프라인 차단**: 저장 직전 로컬 판정 + `SaveBlockReason` 한 갈래 추가(§3-4).
   여기는 서버가 필요 없어 **먼저 넣어도 되는 조각**이다.
7. **이벤트 로그**(§4): 테이블 + 배치 수집 엔드포인트 → 앱 로컬 큐 → 동기화에 얹기.
   개인정보 처리방침 개정이 **선행**이다.
8. **문서**: `docs/spec/voice-and-message.md` 에 차감·수명 규칙을 옮기고, CLAUDE.md 의
   「직접 입력은 문구까지 기억한다」 절에서 **"차감 없이 저장된다"** 문장을 새 규칙으로
   고친다(§3-1 의 ⚠ 3번).

## 6. 정해야 하는 것

1. ~~기기 교체·재설치 후의 차감~~ — **폰 기준으로 확정**(2026-09-07). 가족 알람도 같은
   기준이다(수신자별 생성이라 폰 캐시를 못 쓰므로 사실상 매번 차감된다).
2. ~~이벤트 로그의 보관 기간·처리방침 문구~~ — **1년으로 정했고 반영했다**(2026-09-07).
   처리방침 5판 본문에 항목·목적·보관 기간을 넣고, cron 이 그 값을 지킨다.
   ⚠ 처리방침 **버전은 올리지 않았다** — 5는 아직 `main` 에 없어 동의자가 0명이라
   제자리에서 고치는 것이 규칙이다(`docs/legal/README.md`).
3. **공유 목소리의 보관함.** 커플·가족이 같은 목소리를 쓸 때 서로의 문구를 보여 줄지.
   문구는 개인적인 글이라 기본은 '안 보여 준다' 가 맞다고 본다.
4. **저장 총량 감시.** 서버 보관 상한을 없앴으니 사용자당 오브젝트 수·바이트 계측은
   붙여 두는 편이 좋다.
