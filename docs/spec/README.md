# 동작 스펙 — 세 구현의 **단일 출처**

이 폴더는 AlarmTalk 이 **무엇을 하는가**를 정한다. 안드로이드·iOS·백엔드 셋 다 여기를 따른다.

## 왜 만들었나

플랫폼마다 문서를 따로 두니 같은 규칙이 세 벌로 갈라졌고, 갈라진 줄 아무도 모른 채
한쪽만 고쳐 왔다. 실제로 이렇게 어긋났다:

- 기본 목소리 제한을 안드로이드는 `freeVoiceTier **||** 시스템보이스`, iOS 는 `**&&**` 로
  써서, iOS 에서만 말할 수 없는 문구를 저장할 수 있었다(2026-08-07).
- 목소리 게이트를 양쪽 다 2분기로 써서 **유료 사용자에게 '로그인이 필요해요'** 를 띄웠다.
- iOS 에 「기본 목소리 고르기」 화면이 남아 있었는데, 안드로이드 대응 화면은 **다운로드
  진행 화면**이었다. 소스 주석은 "안드로이드 미러" 라고 주장하고 있었다.

셋 다 "어느 쪽이 맞는가" 를 적어 둔 곳이 없어서 생겼다.

## 규칙

1. **여기가 규칙이고, 코드는 구현이다.** 구현이 스펙과 다르면 **구현이 틀린 것**이다.
2. **동작을 바꾸려면 이 문서를 먼저 고친다.** 코드만 고치고 문서를 두면 다음 사람이
   (사람이든 AI 든) 옛 규칙을 근거로 되돌린다.
3. **각 문서에는 「구현 지도」 표가 있다.** 규칙 한 줄이 세 구현의 어디에 사는지 적는다.
   한 곳만 고치는 사고를 막는 장치다.
4. **소스 주석의 "안드로이드 미러" 주장을 근거로 삼지 말 것.** 근거는 이 스펙이다.
   주석은 썩는다 — 실제로 여러 건이 틀려 있었다.
5. **플랫폼 차이는 「의도된 차이」 절에 적는다.** 거기 없는 차이는 버그다.

## "안드로이드는 이렇게 한다" 고 말하기 전에

⚠ **이 절이 생긴 이유:** 파일 하나만 grep 하고 "양쪽 다 그렇다"·"양쪽 다 빠졌다" 고
단정하는 사고가 **반복해서** 났다. 셋 다 같은 모양이었다:

- 로그아웃 시 알람 예약 취소 → "안드로이드도 없다" 고 했는데 `AlarmRepository`
  에 `detachAlarmsOnSignOut` 이 있었다. 뷰모델만 뒤졌다(2026-08-19).
- 백그라운드 목소리 갱신 → "주기가 그물이다" 라고 **코드 주석에 적었는데** 그 경로에는
  목소리 새로고침이 없었다. 확인하지 않고 썼다(2026-08-19).
- 무료 테마 클립 다운로드 → "테마를 고르는 순간 11개를 받는다" 고 말했는데 실제로는
  캐시 우선이었다. 조사 보고의 요약 문장을 그대로 옮겼다(2026-08-12).

그래서 규칙으로 고정한다:

1. **동작으로 찾는다, 파일로 찾지 않는다.** 한 파일에 없다고 없는 게 아니다. 안드로이드는
   **화면(ViewModel)이 아니라 저장소(`AlarmRepository`)·DAO·워커**에 규칙이 사는 경우가
   많다. 최소한 `apps/android-native/app/src/main/java/` 전체를 훑는다.
2. **없다고 말하려면 "어디를 봤는지" 를 함께 댄다.** "안드로이드에 없다" 가 아니라
   "`X`·`Y` 를 찾아봤는데 없다" 로 적는다. 범위를 밝히면 틀렸을 때 바로 드러난다.
3. **찾은 것은 심볼 이름으로 인용한다.** 요약해서 옮기지 말 것 — 조건부 폴백(`?:`)이
   무조건 경로로 납작해지는 사고가 그렇게 났다.
4. **양쪽에 있어야 할 규칙은 이 폴더의 「구현 지도」에 두 칸 다 채운다.** 한 칸이 비어
   있으면 그게 곧 "한쪽에만 있다" 는 신호다.

## 목차

| 문서 | 다루는 것 |
| --- | --- |
| [alarm-ringing.md](alarm-ringing.md) | 알람이 울릴 때 무엇이 뜨고, 어떻게 꺼지는가 |
| [alarm-editor.md](alarm-editor.md) | 알람을 만드는 화면 — 타임휠, 재생 방식, 모달 세 형태 |
| [voice-and-message.md](voice-and-message.md) | 목소리·문구를 고르고 저장하는 규칙, 등급별 제한 |
| [plan-gates.md](plan-gates.md) | 로그인·이용권 게이트가 뜨는 조건과 문구 |
| [family-alarm.md](family-alarm.md) | 남에게 보내는 알람 — 보내면 끝, 설정 불가능 시간 |
| [consent.md](consent.md) | 동의 화면 — 미체크는 철회가 아니다, 재동의 레버 |
| [gates-and-overlays.md](gates-and-overlays.md) | 차단 게이트와 1회성 오버레이의 순서 |
| [session-and-auth.md](session-and-auth.md) | 로그인 유지 — 한 번 하면 다시 안 한다, 백그라운드 갱신 |
| [billing-lifecycle.md](billing-lifecycle.md) | 구독 해지·만료 — 스토어가 권위, 못 물으면 안 바꾼다 |
| [alarm-lifecycle.md](alarm-lifecycle.md) | 알람의 생애 — 행과 예약은 다른 겹, 계정을 떠날 때 |
| [usage-events.md](usage-events.md) | 사용 기록 — 오프라인에 쌓고 모아 보내기, 울릴 때 네트워크 금지, 보관 1년 |
| [error-codes.md](error-codes.md) | 에러 코드 — 목록은 하나, 코드는 바꾸지 않는다, 기록은 전부·경보는 골라서 |

## 여기 넣지 않는 것

- **어떻게 빌드·배포하는가** → `CLAUDE.md`, `docs/ops/`
- **왜 그렇게 정했는가(법·정책 근거)** → `docs/legal/`
- **한 플랫폼에만 있는 기술 제약의 상세** → `docs/ios/`, 각 앱 README
  (제약이 **동작을 가른다면** 그 결론은 여기 「의도된 차이」에 적는다)
