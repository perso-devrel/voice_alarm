# P26 완료 — 알람 음성 모달, voice status 필터, 알림 권한

## 집은 항목
P26 전체 (4개 항목)

## 변경 사항

### 1. 웹: AlarmsPage 음성 프로필 이름 클릭 → 상세 모달
- AlarmsPage에 VoiceDetailModal import + voiceProfiles 쿼리 추가
- 알람 카드의 voice_name 스팬에 onClick(stopPropagation) → 프로필 매칭 → 모달
- 변경: `packages/web/src/pages/AlarmsPage.tsx`

### 2. 모바일: 반복 요일 프리셋
- 이미 구현 확인: edit.tsx, create.tsx 모두 quickSetDays(매일/평일/주말) 있음

### 3. 백엔드: GET /api/voice status 필터
- `?status=ready|processing|failed` 쿼리 파라미터 추가
- 유효하지 않은 status 값은 무시 (전체 반환)
- 변경: `packages/backend/src/routes/voice.ts`

### 4. 모바일: 설정 알림 권한
- expo-notifications getPermissionsAsync로 현재 상태 확인
- 허용됨/허용 안 됨 표시 + 탭 시 권한 요청 → 거부 시 Linking.openSettings()
- 변경: `apps/mobile/app/(tabs)/settings.tsx`

## 검증
- backend: tsc --noEmit ✓
- web: tsc --noEmit ✓, build ✓
- mobile: tsc --noEmit ✓
