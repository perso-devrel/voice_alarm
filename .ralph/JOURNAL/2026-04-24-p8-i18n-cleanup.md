# 2026-04-24 P8: alarmForm i18n + fontForWeight 정리

## 집은 항목
알려진 이슈 2건 동시 해결:
1. `alarmForm.ts` 검증 에러 메시지 하드코딩 한국어 → i18n 키 전환
2. `fontForWeight` 미사용 함수 삭제 (packages/ui + apps/mobile 양쪽)

## 접근

### alarmForm.ts i18n
- `formatLastSeen.ts` 패턴 차용: `validateAlarmForm(input, t: TFunction)` 시그니처로 변경
- 대안 1: i18n 키만 반환하고 caller에서 번역 → 테스트 편하지만 API 표면 변경 큼
- 대안 2: i18n 모듈 직접 import → React 외부에서 i18n 인스턴스 접근 필요, 결합도 높음
- **채택**: TFunction 주입 (대안 1+2의 장점 결합, 기존 패턴과 일치)

### fontForWeight 정리
- 정의 위치 2곳: `packages/ui/src/tokens.ts`, `apps/mobile/src/constants/theme.ts`
- export: `packages/ui/src/index.ts`
- 사용처: 0건 (P0 Phase 1-B-2에서 전체 파일을 fontFamily 직접 지정으로 변환 완료 후 dead code화)
- 단순 삭제

## 변경 파일
1. `apps/mobile/src/lib/alarmForm.ts` — TFunction import + validateAlarmForm 시그니처 변경, 4개 에러 문자열 → t() 호출
2. `apps/mobile/src/i18n/ko.json` — `alarmValidation.*` 4키 추가
3. `apps/mobile/src/i18n/en.json` — `alarmValidation.*` 4키 추가
4. `apps/mobile/app/alarm/create.tsx` — validateAlarmForm 호출에 t 전달
5. `apps/mobile/app/alarm/edit.tsx` — validateAlarmForm 호출에 t 전달
6. `apps/mobile/test/alarmForm.test.ts` — mock t 함수 추가, 에러 검증을 i18n 키 비교로 변경
7. `packages/ui/src/tokens.ts` — fontForWeight 함수 삭제
8. `packages/ui/src/index.ts` — fontForWeight export 삭제
9. `apps/mobile/src/constants/theme.ts` — fontForWeight 함수 삭제

## 검증
- Mobile typecheck: 통과
- Backend typecheck: 통과
- alarmForm.test.ts: 17/17 통과
- Backend full suite: 538/538 통과
- fontForWeight 잔존 참조: 0건 (소스 코드 기준)

## 다음 루프
- STATE.md 알려진 이슈에서 2건 제거
- 자가 생성 풀에서 다음 항목 선택
