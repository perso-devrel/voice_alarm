# P3: R2 스토리지 연동

## 작업 항목
BACKLOG P3 — R2 스토리지 연동 (음성 파일)

## 접근
1. `packages/backend/src/lib/r2-storage.ts` — `R2VoiceStorage` 클래스 신규 생성 (VoiceStorage 인터페이스 구현)
2. `packages/backend/wrangler.toml` — `[[r2_buckets]]` 바인딩 추가 (VOICE_BUCKET → voice-alarm-voices)
3. `packages/backend/src/types.ts` — `Env`에 `VOICE_BUCKET?: R2Bucket` 추가 (optional — R2 미설정 시 in-memory 폴백)
4. `packages/backend/src/routes/voice.ts` — `getStorage(env)` 헬퍼로 R2 우선, in-memory 폴백

## 변경 파일
- `packages/backend/src/lib/r2-storage.ts` (신규) — R2VoiceStorage implements VoiceStorage
- `packages/backend/wrangler.toml` — r2_buckets 바인딩 추가
- `packages/backend/src/types.ts` — VOICE_BUCKET 추가
- `packages/backend/src/routes/voice.ts` — R2 스토리지 통합

## 설계 결정
- `VOICE_BUCKET`을 optional로 설정하여 로컬 개발(wrangler dev) 시 R2 없이도 in-memory로 동작
- R2 customMetadata에 userId, mimeType, sizeBytes 등 저장 → get() 시 메타데이터 복원
- objectKey 형식: `voices/{userId}/{timestamp}_{counter}`

## 검증
- Backend typecheck: ✅ 통과
- Mobile typecheck: ✅ 통과

## 다음 루프 참고
P3 R2 스토리지 완료. P3 FCM 푸시 구조로 진행.
