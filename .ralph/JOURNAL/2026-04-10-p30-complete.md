# P30 — 메시지 상세 모달 알람 설정 + 기존 구현 확인

## 선택한 BACKLOG 항목
P30 전체 4개 항목

## 접근

### P30-1: 웹 메시지 상세 모달 알람 설정 (구현)
- MessagesPage.tsx의 기존 detailMessage 모달에 알람 설정 폼 추가
- `createAlarm` API 임포트 + alarmMutation 추가
- 모달 하단에 "알람 설정" 토글 버튼 → 시간 입력 + 반복 요일 선택 → 알람 생성
- VoiceDetailModal 패턴과 일관된 UI 스타일 유지

### P30-2: 모바일 알람 탭 시 편집 화면 이동 (이미 구현)
- alarms.tsx:219에 `onPress={() => router.push({ pathname: '/alarm/edit', params: { id: item.id } })}` 이미 존재
- 추가 작업 불필요

### P30-3: 백엔드 gift 페이지네이션 total 정확도 (이미 구현)
- gift.ts의 received/sent 엔드포인트 count 쿼리가 이미 JOIN 포함 (lines 97-101, 137-142)
- 추가 작업 불필요

### P30-4: 모바일 홈 탭 다음 알람 메시지 미리보기 (이미 구현)
- index.tsx:213에 `🗣️ {nextAlarm.voice_name}: "{nextAlarm.message_text}"` 이미 표시
- 추가 작업 불필요

## 변경 파일
- `packages/web/src/pages/MessagesPage.tsx` — createAlarm 임포트, 알람 생성 상태/mutation 추가, 상세 모달에 알람 설정 폼 UI 추가

## 검증 결과
- ✅ 백엔드 typecheck 통과
- ✅ 웹 typecheck + build 통과
- ✅ 모바일 typecheck 통과

## 다음 루프 참고
- P30 전체 완료. 새 P31 항목 생성 필요.
