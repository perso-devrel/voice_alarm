# P13 전체 완료 — 2026-04-10

## 집은 BACKLOG 항목
P13 전체 4개 항목

## 접근 및 변경

### 1. 모바일: 메시지 라이브러리 스와이프 삭제
- **백엔드**: `routes/library.ts`에 `DELETE /:id` 엔드포인트 추가
- **모바일 API**: `api.ts`에 `deleteLibraryItem` 함수 추가
- **모바일 UI**: `library.tsx`에 Swipeable 래핑 + 삭제 확인 Alert + deleteMutation
- **i18n**: ko.json/en.json에 `library.deleteTitle/deleteConfirm/deleteError` 키 추가
- alarms/voices 탭과 동일한 스와이프 패턴 사용

### 2. 백엔드: GET /api/alarm 페이지네이션
- `alarm.ts`에 limit/offset 파라미터 추가 (기본 limit=50)
- COUNT 쿼리 추가, 응답에 total/limit/offset 포함
- friend/gift/library와 동일한 패턴

### 3. 웹: 메시지 목록 삭제 + 낙관적 업데이트
- **백엔드**: `routes/tts.ts`에 `DELETE /messages/:id` 엔드포인트 추가 (message_library 연쇄 삭제 포함)
- **웹 API**: `api.ts`에 `deleteMessage` 함수 추가
- **웹 UI**: `MessagesPage.tsx`에 삭제 버튼 + optimistic update mutation 추가

### 4. 모바일: 알람 생성 프리셋 메시지 선택
- linter/hook이 자동 생성한 코드 기반
- `alarm/create.tsx`에 프리셋 토글, 카테고리 선택, 메시지 선택, 음성 선택, TTS 생성 플로우 추가
- `PRESET_CATEGORIES` 상수 활용 (서버 API 호출 없이 로컬 데이터)
- i18n 키 자동 추가됨 (quickCreate, selectVoice, emptyVoice, presetCategory, generatePreset, ttsError)

## 검증 결과
- 백엔드: `npx tsc --noEmit` ✅
- 모바일: `npx tsc --noEmit` ✅
- 웹: `npx tsc --noEmit && npm run build` ✅

## 다음 루프 참고
- P13 전체 완료. P14 생성 필요.
- 후보: API 응답 타입 강화, 모바일 알람 편집 화면, 웹 알람 생성 기능 등
