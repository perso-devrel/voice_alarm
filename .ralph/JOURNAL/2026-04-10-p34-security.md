---
date: 2026-04-10
slug: p34-security
---

# P34 보안 + 데이터 무결성

## 집은 BACKLOG 항목
- P34: CORS 강화, body size limit, voice DELETE cascade, user/me 확인

## 변경 내역

### 1. CORS 화이트리스트 강화
- index.ts: origin을 배열 → 함수로 변경, 프로덕션 도메인 추가 (voice-alarm.pages.dev, voicealarm.pages.dev)
- maxAge: 86400 (24h) 추가로 preflight 요청 캐싱

### 2. Body Size Limit 미들웨어
- middleware/bodyLimit.ts 신규: Content-Length 기반 512KB 제한, 초과 시 413
- index.ts에 글로벌 적용

### 3. Voice DELETE Cascade
- voice.ts: force=true 삭제 시 alarms → message_library → messages → voice_profile 순서로 정리
- 기존에는 voice_profiles만 삭제하여 orphan 데이터 발생 가능했음

### 4. GET /api/user/me
- 이미 구현 확인 (user + stats 반환)

## 검증 결과
- vitest: 187 tests all pass
- tsc: 통과
- web build: 통과

## 다음 루프 참고
- P35 생성됨 (미들웨어 테스트). rateLimit.test.ts 이미 완료.
