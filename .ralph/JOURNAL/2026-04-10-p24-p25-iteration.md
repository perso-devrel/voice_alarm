# P24 완료 + P25 진행

## 작업 항목
- P24-1: 웹 메시지 삭제 409 응답 처리
- P24-2~4: 이전 루프에서 이미 구현 확인
- P25-1: 라이브러리 삭제는 409 불필요 확인
- P25-3: 모바일 홈 탭 "다음 알람" → 편집/생성 화면 이동
- P25-4: 백엔드 gift API에 검색 파라미터 추가

## 변경 파일
- `packages/backend/src/routes/tts.ts` — voice_profile_id 필터 쿼리 파라미터 추가
- `packages/backend/src/routes/gift.ts` — received/sent에 q= 검색 파라미터 추가
- `packages/web/src/services/api.ts` — deleteMessage에 force 파라미터 추가
- `packages/web/src/pages/MessagesPage.tsx` — 삭제 시 409 처리 (연관 알람 경고 confirm)
- `apps/mobile/src/services/api.ts` — getMessagesByVoice 추가
- `apps/mobile/app/voice/[id].tsx` — 음성 프로필 상세 화면 신규
- `apps/mobile/app/(tabs)/voices.tsx` — 카드 탭 경로 수정 (/voice/[id])
- `apps/mobile/app/(tabs)/index.tsx` — 다음 알람 카드 → 편집/생성 이동
- `apps/mobile/app/_layout.tsx` — voice/[id], alarm/edit 스크린 등록
- `apps/mobile/src/i18n/ko.json`, `en.json` — screen.voiceDetail 키 추가

## 검증
- 백엔드: tsc --noEmit ✅
- 모바일: tsc --noEmit ✅
- 웹: tsc --noEmit + build ✅

## 판단
- P24-2/3/4는 이전 루프에서 이미 구현되어 있었음 (VoiceDetailModal, getAlarm, voice stats)
- P25-1은 라이브러리 삭제가 message_library 테이블만 삭제하므로 409 미해당
- 홈 탭 알람 카드: 알람 있으면 편집, 없으면 생성으로 분기
