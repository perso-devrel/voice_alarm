# P27 완료 — 2026-04-10

## 집은 항목
P27: 웹 MessagesPage 음성 모달, 백엔드 voice_profile_id 필터, 모바일 음성→메시지 바로가기, 웹 테마 영구 저장

## 접근
- P27-1 (MessagesPage 음성 클릭 → 모달): 코드 확인 결과 이미 구현됨 (voice name 클릭 → VoiceDetailModal)
- P27-2 (GET /api/message voice_profile_id 필터): tts.ts에서 이미 지원 확인
- P27-3 (모바일 음성→메시지 바로가기): voice/[id].tsx에 TouchableOpacity 버튼 추가, router.push로 message/create?voice_id= 전달. message/create.tsx는 이미 voice_id searchParam을 받아 selectedVoiceId 초기화하는 코드가 있었음
- P27-4 (웹 테마 영구 저장): useDarkMode.ts에서 localStorage('voicealarm_theme') 키로 이미 영구 저장

## 변경 파일
- `apps/mobile/app/voice/[id].tsx` — TouchableOpacity import 추가, useRouter 추가, "이 음성으로 메시지 만들기" 버튼 + 스타일 추가
- `apps/mobile/src/i18n/ko.json` — voiceDetail.createMessage 키 추가
- `apps/mobile/src/i18n/en.json` — voiceDetail.createMessage 키 추가

## 검증
- backend tsc --noEmit: PASS
- web tsc --noEmit + build: PASS
- mobile tsc --noEmit: PASS

## 다음 루프 참고
- P27까지 모두 완료. BACKLOG 자가 생성 풀에서 새 항목 채워야 함.
