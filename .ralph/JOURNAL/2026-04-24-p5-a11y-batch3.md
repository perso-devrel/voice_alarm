# P5: 스택 화면 접근성 Batch 3

## BACKLOG 항목
스택 화면 접근성 Batch 3 (voice/upload, voice/diarize, character/index, player, voice/[id], friend/[id])

## 접근

### voice/upload.tsx (~4 라벨)
- pickButton: accessibilityRole="button" + 파일 선택 전/후 분기 라벨
- nameInput: accessibilityLabel
- submitButton: accessibilityRole="button" + accessibilityState

### voice/diarize.tsx (~10 라벨)
- pickButton: 녹음 파일 선택 버튼
- analyzeButton: 화자 분리 시작 + disabled state
- speakerCard: 화자 인덱스, 발화 시간, 구간 수 조합 라벨 + selected state
- nameInput: 음성 프로필 이름 입력
- submitButton + backButton(×2): role + label

### character/index.tsx (~12 라벨)
- streakCard: 연속 기상 N일 + 최장 기록 조합 라벨
- MilestoneBadge: 달성/미달성 상태 텍스트 라벨 (이모지 단독 정보 보완)
- StatBar: 능력치명+값 조합 라벨
- XP 통계 3개: label+value 조합
- 섹션 제목 2개: accessibilityRole="header"

### player.tsx (~5 라벨)
- closeButton: 닫기 버튼
- profileSection: 음성 메시지 발신자 라벨
- playButton: 재생/일시정지 상태 분기
- waveformContainer: adjustable role + 진행률 값
- reactionButton: 감사 반응 보내기

### voice/[id].tsx (~10 라벨 + i18n 마이그레이션)
- 하드코딩 한국어 7군데 → i18n 키로 교체 (rename 관련 Alert, 버튼 텍스트)
- avatar, stat items, createMessage 버튼에 accessibilityLabel 추가
- rename row 3개 요소에 accessibilityRole="button" 추가

### friend/[id].tsx (다크모드 수정 + ~10 라벨)
- **다크모드 버그 수정**: `Colors.light` 하드코딩 → `useTheme()` + `createStyles(colors)` 패턴
- StatCard 컴포넌트 타입을 `ThemeColors`로 변경
- backButton, avatar, statCards, gift items, action buttons 전체 접근성 라벨 추가
- section titles에 accessibilityRole="header" 추가

## i18n 추가 키 (43개, ko+en)
- voiceUpload: a11yPickFile, a11yFileInfo, a11yNameInput, a11ySubmit
- voiceDiarize: a11yPickFile, a11yAnalyze, a11ySpeaker, a11yNameInput, a11ySubmit, a11yBack
- voiceDetail: a11yAvatar, a11yRename, a11yRenameInput, a11yRenameSave, a11yRenameCancel, a11yStat, a11yCreateMessage, renameFailed, renameNetworkError, renameInputError, renameSaving, renameSave, renameCancel, rename
- player: a11yClose, a11yPlay, a11yPause, a11yWaveform, a11yReaction, a11yVoice
- character: a11yStreak, a11yMilestone, a11yMilestoneAchieved, a11yMilestoneNotYet, a11yStat, a11yXpStat
- friendProfile: sendGift, a11yBack, a11yAvatar, a11yStat, a11ySetAlarm, a11ySendGift, a11yGiftItem

## 변경 파일 (8개)
- `apps/mobile/app/voice/upload.tsx` — 접근성 라벨 4개
- `apps/mobile/app/voice/diarize.tsx` — 접근성 라벨 10개
- `apps/mobile/app/character/index.tsx` — 접근성 라벨 12개
- `apps/mobile/app/player.tsx` — 접근성 라벨 5개
- `apps/mobile/app/voice/[id].tsx` — 접근성 라벨 10개 + 하드코딩 한국어→i18n
- `apps/mobile/app/friend/[id].tsx` — 다크모드 수정 + 접근성 라벨 10개
- `apps/mobile/src/i18n/ko.json` — 43개 키 추가
- `apps/mobile/src/i18n/en.json` — 43개 키 추가

## 검증
- `npx tsc --noEmit` 통과 (mobile)

## 다음 루프 주의사항
- P5 접근성 Batch 3 완료. 남은 접근성 항목: 이모지 단독 정보 전달 점검, 한국어/영어 레이아웃 점검
- friend/[id].tsx의 다크모드 수정은 P5 다크모드 작업의 누락분 보완
