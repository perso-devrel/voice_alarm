# P25 완료 — 대시보드 음성 모달 연동

## 집은 항목
P25: 웹 대시보드 최근 활동에 음성 프로필 클릭 시 상세 모달 연동

## 접근
- VoiceDetailModal을 VoicesPage에서 export하여 DashboardPage에서 재사용
- DashboardPage에 voiceProfiles 쿼리 추가, voice 타입 활동 클릭 시 ID로 프로필 매칭 → 모달 열기
- 프로필을 찾지 못하면 기존 동작(voices 페이지 이동)으로 폴백

## 변경 파일
- `packages/web/src/pages/VoicesPage.tsx` — VoiceDetailModal에 export 추가
- `packages/web/src/pages/DashboardPage.tsx` — useState, VoiceDetailModal import, voiceProfiles 쿼리, 클릭 핸들러 분기, 모달 렌더

## P25 나머지 항목
- 모바일 홈 "다음 알람" 카드 → 알람 편집: 이미 구현 확인 (index.tsx:202)
- 백엔드 gift 검색 파라미터: 이미 구현 확인 (q= 파라미터, received/sent 모두)

## 검증
- backend: tsc --noEmit ✓
- web: tsc --noEmit ✓, build ✓
- mobile: tsc --noEmit ✓

## 다음 루프
P26 생성 완료. 첫 항목: 알람 목록에서 음성 프로필 이름 클릭 시 상세 모달
